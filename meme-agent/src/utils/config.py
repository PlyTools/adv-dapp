import os
from dotenv import load_dotenv

def load_configuration():
    """Load environment variables and other configurations"""
    load_dotenv()
    # Add any additional configuration loading here if needed 